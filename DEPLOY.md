# 部署公开模板

本仓库用于独立的作品集演示服务。默认启动 `demo.app:app`，根地址进入 `/demo/`；原私人 API 路径返回 404。

## 本地

按 README 安装后执行 `bash start.sh`。默认仅本机可访问。首次安装依赖需要网络；已有环境更新依赖请重新执行 `.venv/bin/python -m pip install -r requirements.txt`。

## 容器

```bash
docker compose up --build
```

浏览器打开 http://localhost:8000/。容器只复制代码和静态资源，不挂载私人 `data/`。本次未执行容器构建；本地 Python 入口已验证。

## 独立托管

将这份代码作为新的作品集仓库部署至支持 Python/容器的平台；不要覆盖已有私人服务。容器启动命令已使用模板入口，健康检查为 `/demo/health`。`render.yaml` 为可选的服务配置；地区、套餐、容量与价格应在实际部署时确认，本说明不承诺免费或稳定在线。

不需要配置 `BACKUP_GITHUB_TOKEN`、`BACKUP_GITHUB_REPO`，不挂载私人数据库或上传目录。模板数据每次启动重建，使用单进程、单实例；多实例会形成各自的示例状态，不应误认为共享持久存储。

部署后检查：根地址跳转、模板登录、图片、四段回忆展开；确认 `/api/places` 和 `/media/...` 不在公开入口开放。访客使用共享示例账号，写入可能互相可见；这不是用于真实回忆的托管产品。

当前交付为本地可运行仓库和压缩包，未执行 GitHub 推送或公网部署。
