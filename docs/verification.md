# 本次验证记录

## 范围

本次作品集整理在 2026-09-14（用户本地时区）开始。使用 Python 3.13 环境及原项目已安装依赖：FastAPI 0.141.1、Uvicorn 0.52.3、Pillow 12.3.0。仅复制应用代码，在临时目录中运行示例数据；未读取私人数据库、照片或备份。

## 自动检查：32 项通过

运行 `scripts/verify_demo.py`，服务通过本机 HTTP 访问。脚本每次创建临时应用副本，结束后清理；只操作程序生成的图片和虚构文字。

- 根地址跳转模板，5 条原私人入口被阻止。
- 未登录 Demo API 被拒绝，静态脚本与地球贴图可加载。
- 两个示例账号可登录，种子数据为 7 段回忆、7 张图片。
- 匿名访问图片被拒绝，登录后可访问。
- 非作者修改和删除地点返回拒绝；模板新注册被阻止。
- 带图片的回忆可新增、可加载缩略图、可由作者编辑与删除。
- 留言可关联回忆，另一示例成员可见并有未读提示。
- 删除回忆后留言仍在，关联置空。
- 退出后认证访问失效。
- 启动及重启均未改变临时“私人目录”中的哨兵文件，也没有在其中新建数据库。
- 重启后恢复 7 段回忆和 2 条示例留言。

复现：先按 README 安装依赖，再在仓库根目录运行：

```bash
.venv/bin/python scripts/verify_demo.py
```

## 实际界面检查

使用本机浏览器打开模板入口，确认虚构内容横幅、地球贴图、7 条时间线、回忆详情、四段拍立得展开，以及新增表单。保存 4 张实际截图，见 [演示路线](../demo/README.md)。

浏览器检查覆盖展示与导航，记录写入和权限检查通过 HTTP 执行。没有把浏览器只打开表单描述为完整 UI 上传流程验收。

## 源码一致性

本副本的 `app/` 与 `static/` 所有交付文件均与源仓库工作区逐字节一致，`requirements.txt` 保持原版本。SHA-256 清单见 `source-manifest.json`。新增入口、默认启动、部署配置和文档是本次整理改动。

## 未验证项目

- 全新环境下载依赖、Docker 构建、托管平台部署。
- 外部地名搜索完整正确性、断网场景的全部交互。
- 真实手机触控、无障碍、低性能设备与多人并发。
- 原私人备份恢复和生产数据可靠性。
- 真实用户需求、留存、满意度和效率改善。

这些检查证明受测功能与模板边界，不代表完整安全审计、生产认证或产品价值验证。
